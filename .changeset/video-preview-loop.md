---
"expo-media-viewer": minor
---

Replace the image-first wrapper API with a media-first `items` + render-prop API, add package-owned image/video thumbnails with default video indicators, and support request headers for media and thumbnail URLs.
